type DisposalBin @model {
  binId: String!
  name: String
  location: [Float]
  hits: Int
  type: String
  dateOfInit: AWSDateTime
}

type BinRecord @model {
  id: ID!
  binId: String!
  date: AWSDateTime!
  action: String
}